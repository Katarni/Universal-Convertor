#include "App.h"

int main(int argc, char *argv[]) {
//  QApplication a(argc, argv);
//
//  App convertor(600, 500);
//  convertor.render();
//
//  return QApplication::exec();

//  std::cout << Convertor::convert("0.125", 7, 10);
  std::cout << Number("1", 10) / Number("343", 10);
}
